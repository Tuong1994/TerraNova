import { domain } from "../../configs/setting";

export const descriptionPaths = {
    createDescription: `${domain}/api/descriptionManagement/createDescription`,
    removeDescription: `${domain}/api/descriptionManagement/removeDescription`,
}