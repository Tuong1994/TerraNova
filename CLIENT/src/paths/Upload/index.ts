import { domain } from "../../configs/setting";

export const uploadPaths = {
    uploadCourseImg: `${domain}/api/uploadManagement/uploadCourseImg`,
    uploadAvatar: `${domain}/api/uploadManagement/uploadAvatar`,
}